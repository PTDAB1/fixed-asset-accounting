CREATE TABLE assets (
    id SERIAL PRIMARY KEY,
    inventory_number VARCHAR(50) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    model VARCHAR(100),
    serial_number VARCHAR(100),
    purchase_date DATE,
    initial_cost DECIMAL(12,2),
    current_cost DECIMAL(12,2),
    status VARCHAR(50),
    useful_life INTEGER,
    location_id INTEGER,
    responsible_id INTEGER
);

CREATE TABLE operations (
    id SERIAL PRIMARY KEY,
    asset_id INTEGER REFERENCES assets(id),
    operation_type VARCHAR(50),
    operation_date DATE,
    document_number VARCHAR(100),
    amount DECIMAL(12,2),
    notes TEXT
);

INSERT INTO assets (inventory_number, name, initial_cost, status) 
VALUES ('INV-001', 'Сварочный аппарат Lincoln Electric', 15000.00, 'В эксплуатации');
